apply plugin: "war"

bootWar {
    mainClassName = "br.com.sabino.lab.SabinoLabsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
